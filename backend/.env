# =============================================================================
# ENVIRONMENT CONFIGURATION FOR PROYECTO_0 BACKEND
# =============================================================================

# Server Configuration
PORT=8080
HOST=0.0.0.0
GIN_MODE=debug

# JWT Configuration  
JWT_SECRET=your-super-secret-jwt-key-change-me-in-production
JWT_ISSUER=Proyecto_0
JWT_EXPIRATION=24h

# Application Configuration
APP_NAME=Proyecto_0
APP_VERSION=1.0.0
APP_ENV=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# 
# RECOMMENDED: Use PostgreSQL (required for profile pictures and full features)
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=proyecto_0
DB_USER=marilyn
DB_PASSWORD=
DB_SSL_MODE=disable
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# ALTERNATIVE: In-memory database (development/testing only)
# NOTE: Data is lost on restart, profile pictures won't persist
# DB_DRIVER=memory

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# 🐳 DOCKER COMPOSE (Recommended):
#   Use: docker-compose -f docker-compose.local.yml up
#   Database: Automatically configured PostgreSQL
#
# 🔧 LOCAL DEVELOPMENT:
#   1. Start PostgreSQL manually or use existing instance
#   2. Set DB_* variables above to match your PostgreSQL setup
#   3. Run: go run cmd/api/main.go
#
# ⚡ QUICK TESTING:
#   Use DB_DRIVER=memory for quick tests (data won't persist)